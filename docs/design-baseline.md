# 扣扣的家设计基线

设计基线日期：2026-08-31；2026-09-03已按当前源码刷新覆盖表。本文仍不代表真实微信账号或生产环境验收，实时差异以[code audit](implementation-gap-audit.md)为准。

## 用户要求与当前工程方案

用户明确要求与乙方建议的来源分类，以[实施契约](design/contract-details.md)为准。以下包含工程方案，不代表用户已确认每个技术细节。

1. 微信只负责账号身份登录；家庭角色、模块权限和行程权限由本系统后台维护。
2. 账号可以拥有多个角色。厨师是餐饮模块的子角色；露营协作者不自动获得所有行程访问权。
3. 只有显式加入某次行程的成员可以访问该行程。完成后保留历史成员关系，使参与者继续查看历史内容。
4. 行李模板完全由用户自定义。系统不内置“烧烤模块”，也不得因为模板名称是“烧烤”而自动填充物品或触发特殊逻辑。
5. 套用模板时复制为 `TripPackingItem` 行程快照；模板后来修改、归档或删除，不改变已生成的行程清单。
6. 一个行程可以套用多个模板并手工追加本次物品；重复套用按来源模板项标识去重。
7. 行李负责人必须是该行程成员；一期准备状态为 `PENDING` 和 `PACKED`。
8. 菜谱不记录难度、忌口和营养；调料不记录用量。
9. 库存比对仅作辅助判断，充足、缺少、未知全部展示并使用不同颜色；用户负责最终确认。
10. 不建设家庭账本、预算、费用分摊或家务积分排名。

## 生产架构

- 客户端：uni-app + Vue 3 + TypeScript。
- API：NestJS + Fastify + Prisma。
- 数据库：PostgreSQL 17；出现真实空间查询需求后再启用 PostGIS。
- 缓存/任务：Redis 7；通知模块启用 BullMQ。
- 对象存储：腾讯云 COS 北京私有桶。
- 边缘入口：Nginx + Certbot。
- 正式 API：`https://pp6v4.com/api/v1`，不新增 `api.pp6v4.com`。

## 当前代码覆盖

| 领域 | 状态 | 说明 |
|---|---|---|
| 登录、家庭创建 | 已有主流程 | 仍需真实微信双账号验收 |
| 菜谱、点餐、库存、购物 | 主流程已实现 | 库存按用户决定仅人工维护；仍需双账号与真机验收 |
| 日历 | 主流程已实现 | 来源跳转、一次/周期纪念日、2月29日策略已实现；待数据库与真机验收 |
| 行程基础 | 主流程已实现 | 地图供应商真实道路规划和真机效果待补 |
| 自定义行李模板 | 主流程已实现 | 来源名历史快照、小组分工、模块权限、模板版本与删项软归档已实现；待隔离库和真机验收 |
| 邀请与权限管理页 | 已实现 | 仍需真实微信多账号验收 |
| 待办 | 已实现 | 提醒计划已保存，可靠通知发送尚未实现 |
| 收藏与灵感 | 已实现 | 手工图文/链接、可见性、归档、私有图片和草稿转换；独立数据库及真机待验收 |
| 家庭档案 | 已实现 | 字段ACL、加密、显式查看审计和归档；生产密钥与真机待验收 |
| 非财务生活看板 | 已实现 | 按来源权限聚合并处理空分母；独立数据库口径与真机待验收 |
| 通知 | 站内主链路已实现 | outbox/worker/Inbox/偏好与订阅回执已完成；微信正式发送待模板联调 |

## 下一验证顺序

1. 在隔离PostgreSQL执行全部迁移和HTTP回归。
2. 用真实微信双账号验收邀请、权限、点餐、行程与照片。
3. 配置COS、档案密钥、微信订阅模板和地图Key后完成外部联调。
4. ICP与HTTPS就绪后部署，再执行备份恢复和生产健康检查。

## 当前发布门槛

除了常规功能开发，还需验证家庭请求头缺失时不得跨域查询、日历与看板不得泄露非成员行程、Docker镜像排除.env与密钥、API受控外网出口、COS签名撤权时间窗。这些是源码审查与设计发现，未在本轮直接修改生产环境。

设计评审项R01（确认后改单）、R02（主动撤权与历史访问）、R03（档案字段与工单用途）已于2026-08-31确认，详见[确认及授权记录](design/decisions-20260831.md)。本次仅授权虚构数据本地预览，不执行真实成员权限变更或生产操作。

## 页面交互补充

[页面与交互设计](design/interaction-design.md)已列出P01～P36页面与状态；[行李原型](design/packing-prototype/index.html)以本地内存演示自定义模板、套用预览、独立清单与分工。13条原型模型测试通过，浏览器检查记录见[原型验证](design/prototype-verification.md)。这些结果不表示生产代码已经补上历史来源名称、小组分工或完整权限。

[全页面视觉评审](design/visual-design-review.md)补齐36页视觉稿和七种共用状态。8条设计测试通过，既有36页窄屏DOM与252组状态记录保留；连续点击曾受自动审核拦截，取得本地预览授权后已恢复并通过日历到购物的四步链路及五个主入口检查。R01～R03已确认。真实地图/照片服务和Word逐页版式仍未完成核验；本地设计验证不等于生产业务验收。
