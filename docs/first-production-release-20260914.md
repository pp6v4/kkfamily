# 首个生产版本闭环检查（2026-09-14）

## 当前结论

本批首发候选版已部署生产服务器。本人真机登录、创建家庭并落库已确认；配偶共享家庭、真实 COS 图片和其余真机业务验收仍未完成。服务器部署不等于微信正式版已发布，不得以测试账号、内存图片驱动替代生产验收。

## 范围保持

- 手动维护食材/调料，点餐只辅助比较；做饭不扣库存、购物不自动入库。
- 微信订阅延期，保留站内待办提醒；不要求当前提供订阅模板。
- 同域名 `https://pp6v4.com/api/v1`，由 Nginx 转发，不新增付费域名。
- 家人共享家庭，朋友按角色授权；露营还必须具有对应行程成员身份。

## 本批代码与测试

代码提交 `2f3907973db29ea90bbd20c56584166dbf7d42fe`，已推送 GitHub main。

修复提醒已读接口：同一 Serializable 事务内核对消息收件人、家庭权限、待办权限、当前负责人及待办状态；已读重试也重新授权。撤销待办权限返回403，非本人消息或来源失效返回404，版本冲突返回409，不擅自修改提醒状态。

| 检查 | 结果与环境 |
| --- | --- |
| 后端编译、lint、diff空白检查 | 通过，本机源码 |
| 提醒单元测试 | 7/7；新增事务、幂等重试、撤权、来源丢失、版本冲突、更新失败分支 |
| 隔离 PostgreSQL 回归 | 18次迁移、状态与schema差异检查通过；原PATCH 52/52，POST兼容52/52；包含真实数据库的权限与待办来源校验 |
| 其他隔离测试 | 旧库升级6/6、行程5/5、真实Controller传输兼容2/2 |
| 客户端质量 | 同批既有248/248、lint、vue-tsc通过；本次未改客户端业务源码，复用[已保存证据](batch-20260914.md) |
| Linux严格锁定构建 | 验证镜像使用 `--frozen-lockfile` 安装并编译通过；不宣称依赖兼容警告已全部消除 |
| 小程序构建及交付 | 14:57:49 +08:00，78个文件SHA256一致、14个页面文件完整，原开发者工具私有配置保留 |

隔离证据：`.codex-tmp/remote-db-verification/20260914T065632Z-9bf70082/`；数据库运行ID `83ded9b823865215`，未连接生产库执行业务测试。

## 用户使用哪个测试版本

继续打开 `C:\Users\10353\Documents\Codex\2026-08-26\new-chat\outputs\mp-weixin`，点击“编译”，从“我的家 → 账号与家庭”登录。首次可能自动进入“欢迎回家”。不要导入外层 outputs 目录或旧 mp-weixin-v1.3 文件夹。

客户端版本号目前0.1.0；这是接生产接口的待验收版本，尚未上传微信平台、提交审核或正式发布。

## 真实闭环待验收清单

| 流程 | 通过条件 | 当前缺口 |
| --- | --- | --- |
| 微信登录/家庭共享 | 本人创建家庭、配偶通过邀请码加入同一家庭、刷新后双方数据一致；朋友仅获授权模块 | 15:06:17 +08:00生产只读核验：user/household/membership均1，“扣扣的家”成员ACTIVE、角色ADMIN。本人真机链路已过；配偶及朋友待验 |
| 菜谱→点餐→采购 | 真实封面上传、发布菜谱、两人点同餐汇总、核对数量与手动库存、导入购物去重、购买记录保留且库存不自动变化 | 生产MEDIA_DRIVER=disabled，COS密钥未配置；不放宽封面必填来绕过 |
| 露营协作/照片 | 模板复制到行程、分配负责人、历史成员可查看、非成员拒绝、旅中及结束后照片可保存/读取 | 真实COS与多人验收待完成 |
| 地图 | 地点选择与GCJ02坐标、待出行闪烁虚线、已完成实线箭头在真机验证；示意连线不能冒充道路导航 | 真机未验；道路规划服务未接通，不计作已完成 |
| 待办/日历/收藏/档案 | 真实账号创建并刷新可读取，站内提醒按负责人和权限可见；档案拒绝未授权账号 | 隔离自动化通过不替代真实操作 |
| 微信平台发布 | 当前AppID一致、后台配置/隐私声明等实际核验、真机通过、上传及审核通过后发布 | 尚未执行；本记录不声称取得平台审核结果 |

服务器COS配置由用户在 `/opt/family-life/apps/api/.env` 私下填写；后续核验桶名与地域、设置 `MEDIA_DRIVER=cos` 并随集中部署生效。密钥不进入聊天、客户端、文档或Git。

## 运行镜像与部署

15:05:38 +08:00部署完成，发布代码 `2f39079`。

- 镜像 `kkfamily-api:2f39079`，摘要 `sha256:fd052258c949635f74b912a332ee59925e7026e7d91e25071b5be544c08d95ed`。
- 隔离正式模式启动检查 `runtime-smoke-e7aff10748aca0f9.json` 通过：默认启动命令应用18次迁移，API与worker启动，health200、三个受保护入口未登录401。
- 部署前备份目录 `/home/ubuntu/kkfamily-release-20260914T070421Z-6e6b9307`。数据库备份SHA256 `08ad4565fa18e805b093f8cfa23592d03ac825df0a91360144267e4afdf03721`；旧镜像标签 `kkfamily-api:rollback-20260914T070421Z-6e6b9307`。不自动回滚数据库。
- 生产18次既有迁移校验和一致，schema差异检查通过；未新增迁移、未清空数据。
- 切换后HTTPS健康接口200且status=ok，未登录菜谱401；API与worker均为上述摘要，15:06复核运行正常、重启次数0。
- 生产镜像内提醒单元测试7/7通过（不写生产库）；行程单元5/5通过。
- 24个POST兼容入口经实际HTTPS/Nginx逐项返回未登录401，未写生产业务数据。
- 本地部署证据 `.codex-tmp/production-release/20260914T070421Z-6e6b9307/release.json`。

本人账号15:03:44登录、15:04:11创建家庭；部署后只读查询确认记录保留。查询未读取OpenID、令牌或密钥，未替用户创建任何生产业务数据。
