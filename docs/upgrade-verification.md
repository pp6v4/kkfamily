# 备份恢复与旧数据升级验证（2026-09-09）

## 真实备份验证

- 生产库当前只执行了最初3批迁移，线上API仍为旧镜像，未执行本轮新增迁移。
- 备份留在服务器私有目录 `/home/ubuntu/kkfamily-upgrade-20260908T222137Z-fd336034/production.dump`，目录700、备份600，没有下载生产数据到本机。52589字节，SHA256 `92568d45f133bf65810b392bc681541592cdd7704738634a2f66d30fe3c975d0`。
- 归档目录检查成功；恢复到独立internal网络的PostgreSQL17数据库后，3→18批迁移和模型结构检查通过。原23张业务表均为0行，因此这个结果仅证明真实备份可恢复和该空数据基线可升级，不能据此声称覆盖了已有业务数据转换。
- 脱敏报告：`.codex-tmp/production-upgrade/restore-65d513fae42a7f8f.json`。恢复时未发布公网端口，没有挂载生产数据库卷；演练后只停止副本容器，生产库未变。
- 第一次恢复在PostgreSQL临时初始化进程退出时遭遇socket不存在；健康检查已改为TCP localhost，以避免把仅开放Unix socket的临时初始化进程当作正式服务就绪。

## 带数据升级回归

默认隔离验收新增独立 `verify_legacy` 数据库，仅允许在已经通过隔离地址校验的PostgreSQL容器内创建。先执行最初3批SQL并登记迁移，再插入虚构数据，随后升级到完整模型；不复用生产库或主HTTP用例数据库。

覆盖5个场景：

1. UTC存储的跨日与闰日纪念日转换为上海日期，标题、归属、创建者映射保留，旧投影移入Anniversary。
2. 旧中文餐次规范化：UTC 2026-09-01 23:30对应上海2026-09-02早餐，两票仍保留且聚合成一道菜。
3. 食谱200g、库存150g保持不变；库存标记PRESENT，调料链接补齐为SEASONING。
4. 两名行程成员保留，按原规则确定唯一OWNER，不丢失其他成员。
5. 账号、购物数量和下次超市列表保留；再次migrate deploy不重复纪念日或菜品。

回归先复现旧纪念日SQL将日期提前一天。根因是对不含时区、实际存UTC的TIMESTAMP直接使用Asia/Shanghai解析。修正为先按UTC解释，再转上海日期。

**迁移修改边界：** 已核对本项目生产迁移账本，`202609030003_anniversaries`尚未执行，因而在首次生产执行前修正此文件；已经在本项目生产执行的最初3批文件未修改。若其他环境已执行旧版本纪念日迁移，不得直接覆盖账本、重置数据库或假定日期能推算回来，须使用迁移前原始事件/备份制定数据修复方案。

## 最终结果与证据

- 快照 `20260908T223148Z-76af697f`，Compose项目 `kk-verify-db481a2d88476078`。
- 18批迁移、模型结构无差异、旧数据5个子场景（含父测试6/6）、通知6/6、HTTP接口50/50全部通过，无跳过。
- 镜像manifest：`sha256:7b34dd0231d4b87f919c2a289679694e946c90a16a84505d0d1f040bf81d4524`。
- 完整TAP、阶段结果及逐源文件SHA256在 `.codex-tmp/remote-db-verification/20260908T223148Z-76af697f/`。前一轮日期回归失败证据保留于快照 `20260908T223027Z-ee670cbe`。

## 生产配置状态

微信AppID/AppSecret和JWT已配置（只检查存在性，没有输出秘密）。媒体驱动仍disabled，COS SecretId/SecretKey与订阅提醒模板未配置。档案32字节加密密钥已在服务器生成、写入API环境文件并另存私有恢复副本，但尚未重载业务容器；另需建立服务器外安全备份，防止整机丢失时无法恢复档案密文。

以上验证不是生产版本发布。后续部署前重新取生产备份，检查已应用迁移校验和，使用本轮通过的源码构建业务镜像，保留旧镜像与配置回退入口，再验证API和worker。COS真实写读、微信双账号/真机、地图和订阅消息联调仍未完成。
