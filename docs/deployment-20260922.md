# 2026-09-22 行程照片独立权限发布

## 已完成

- 业务提交：`a65e884`，已推送GitHub main。
- 生产切换完成：2026-09-22 10:26:42 +08。
- API与worker镜像：`sha256:20da27909abeef5503bc6d4699025d0eb74132ac619a29343397417e2924e4a1`。
- 独立照片授权：行程canEdit与photoAdd分别设置；家庭trips:EDIT仍是上传前置权限。只读行程成员可加照片，但不可修改行程；撤销照片权限后旧上传申请不能确认。
- 迁移新增photoAdd并从旧canEdit回填，不修改已执行迁移，不自动变更家庭角色权限。

## 验证证据

隔离快照：`20260922T021828Z-8d3ce574`；隔离项目：`kk-verify-c052614a5ca98308`。
19项迁移、结构一致性、旧数据升级均通过；memory/native、memory/POST、mounted/native、mounted/POST四组HTTP各55/55。
新增用例逐项验证只读成员上传、确认、读取图片像素，以及撤权后确认失败且无新MediaAsset入库。
客户端lint、类型检查、256项测试通过，小程序构建成功。
生产模式冒烟：`e21a81844050efa8`，迁移名称集合与快照逐项一致，API健康200、未登录业务接口401，worker启动通过。

生产部署后API/worker均running、restarts=0，镜像摘要一致；公网HTTPS健康检查通过、未登录菜谱401；COS挂载校验通过。
真实数据只读检查：1个用户、1个家庭、1个有效管理员；菜谱、餐点、购物项、行程均0。没有写入演示业务数据。

## 备份与回退边界

- 备份目录：`/home/ubuntu/kkfamily-release-20260922T022530Z-6f2c0ebb`。
- 数据库备份SHA256：`232b1edc14b26feeadbf16611de50575607ee9812f9e8f1a019c2e339d7a46f4`。
- 旧镜像：`sha256:a1248416ba67c843941a1c2c9796407e36a7fb3fd1829e4f0f701b8e4bdcb9f2`。
- 保留原环境配置及COS挂载override；未自动回滚数据库、未删除用户数据。
- 如需回退，必须同步评估新客户端与旧后端权限语义；不要仅回退镜像后继续使用新客户端。数据库恢复属于另外授权的破坏性操作。

## 客户端交付与剩余边界

2026-09-22 10:26:53 +08同步到原导入目录`outputs/mp-weixin`：78文件、14页面，SHA256不一致0，保留project.private.config.json。
本次是服务器部署及本地小程序产物交付，不等同微信平台正式发布。
用户暂缓真机及双账号验收：原生微信授权、图片展示、实际跨账号协作仍待验收，不能以自动测试替代。
微信平台上传、审核、发布未执行。自动库存扣增、账本预算、微信订阅提醒和道路导航继续遵守已确认的一期范围，不补回延期功能。
