# 自定义行李原型验证记录

日期：2026-08-31。对象仅为docs/design/packing-prototype，不是apps/client或现网API。原型内所有“保存”只写当前页面内存，不写数据库、文件或浏览器持久存储。

## 自动模型测试

执行：`node --test docs/design/packing-prototype/model.test.cjs`。13条通过、0失败、0跳过。以下是逐项期望，并非只验证脚本不报错。

| 测试 | 已核对的具体结果 |
| --- | --- |
| 空初态和任意名称 | templates=[]、items=[]；“烧烤”等4种名字仅保存输入1/2/3，无预置物品 |
| 同来源重复套用 | 第一次新增3；第二次新增0、跳过3；总数3 |
| 不同模板同名物品 | 两份“水”来自不同ID，保留2项 |
| 模板变更与归档 | 行程物品及历史来源名称逐字段与变更前一致 |
| 本次清单独立修改 | 本次可改2.5件；模板不变；sourceItemName仍是原值 |
| 稳定标识与顺序 | 换序后原ID保留；新增物品下次套用只新增1项 |
| 排除与恢复 | 排除1项后有效2项；重复套用新增0；显式恢复变3项且待准备 |
| 责任校验 | g2/m3有效；跨组、非行程成员、外部组全部拒绝且状态不变 |
| 只读成员 | 包括已指定负责人，9种写操作全部拒绝，完整快照不变 |
| 输入校验 | 空名、0、负数、指数、超精度/超上限、重复ID拒绝且状态不变 |
| 多模板原子性 | 后一个模板无效时，前一个也不部分写入 |
| 手工物品 | 不生成模板；数量空白不变成0；未分配、待准备 |
| 快照隔离 | 修改返回对象不改变模型内部模板 |

## 浏览器交互检查

使用真实页面控件输入和点击；未从控制台直接修改原型模型以伪造操作。检查发生于本地回环地址，不访问生产系统。

| 检查 | 实际观察 |
| --- | --- |
| 创建“烧烤”与1/2/3 | 卡片显示3件及1/2/3，本次行李仍0项 |
| 预览与首次套用 | 预计新增3、跳过0；确认后准备进度0/3 |
| 再次预览与取消 | 新增0、跳过3；加入按钮禁用；取消可退出 |
| 准备小组/负责人 | 选朋友家后仅列朋友丙；选择后显示为已选 |
| 勾选准备 | 第1项已准备，进度1/3 |
| 只读演示 | 准备复选框禁用，套用按钮数量0 |
| 修改模板 | 新模板“周末小包/新的物品1”；本次仍“1/来自模板：烧烤” |
| 手工加项 | 新增“临时外套”，来源“本次手工添加”，无模板生成 |
| 错误与草稿 | 数量-1出现字段校验提示；取消→继续编辑保留-1；改为2保存后关闭表单 |
| 无权访问 | 主内容不再出现示例行程名称、成员或清单 |
| 加载 | 显示加载状态，不填充临时业务记录 |
| 读取失败与重试 | 保留现有演示物品并标未更新；重试明确为模拟恢复 |
| 页面脚本日志 | 检查时无error/warn日志 |
| 窄屏与桌面 | 320、390、1280px视口检查；文档scrollWidth与clientWidth相等，无横向页面溢出；目视检查圆角卡片、表单与按钮 |

检查中修正：取消编辑改用页面内的继续/放弃提示；套用按钮缩短为“套用模板”以避免窄屏单字换行。辅助技术名称仍为“套用自定义模板”。

浏览器操作工具在复选框切换后因可访问名称改变曾报选择器失效；重新读取页面确认实际状态已变为1/3，没有再次盲目勾选。清空字段操作也曾未清除值，按实际DOM值核实后改填合法数量2并验证保存。这两项不被当作业务测试通过的依据，实际页面结果才是依据。

## 尚未证明的内容

- 原型模型是设计说明，不是生产权限防线。真实双账号、并发请求、PostgreSQL迁移、COS、地图、微信订阅及真机均未由本测试覆盖。
- 42条A编号业务用例仍NOT_RUN；原型测试不会改写业务验收状态。
- P01～P36已补页面规范，但本次可交互原型只覆盖行李核心流程，不能声称36页全部有高保真效果或已实现。
- 八份Word现有结构审计记录为PASS；LibreOffice缺失，Word逐页渲染视觉审计仍未完成。浏览器原型目视检查不代替Word版式审核。
- R01～R03及农历生日一期范围等待产品评审；无真实成员权限变更、无新增高敏档案收集。
