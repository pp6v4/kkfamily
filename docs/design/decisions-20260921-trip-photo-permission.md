# 行程照片独立权限（2026-09-22已部署）

业务提交a65e884已部署，客户端原导入目录已同步；四组HTTP各55/55、19项迁移和生产启动检查通过。见[部署证据](../deployment-20260922.md)。以下开发过程中的待执行描述保留作为历史记录，不代表仍未部署。

D10要求只读行程成员可被单独授权加照片，当前生产实现却将照片与canEdit绑定。
新增TripMember.photoAdd，家庭trips:EDIT与有效/历史行程成员校验保持不变；photoAdd只允许照片，不授予行程/节点/行李写权限。

迁移新增字段并从旧canEdit回填，以保持此前照片能力；不改旧迁移。创建者photoAdd=true。
新邀请未显式填写photoAdd时沿用canEdit的默认值；之后分别设置，不把修改编辑权限自动当作修改照片权限。
重新邀请已撤销成员重新赋权，不保留旧授权。撤销照片权限后，旧上传申请在上传和确认时都需重新校验。

已完成后端字段、迁移、DTO和权限条件，以及前端独立开关和原生选图返回后的权限重验。
本地后端构建通过；客户端 lint、类型检查与256项测试全部通过，包括只读成员加照片、编辑权限与照片权限分别撤销、过期版本及隐藏页面拒绝提交。
2026-09-22隔离数据库任务20260922T021828Z-8d3ce574退出码0：新HTTP集成用例与19项迁移完成，旧数据升级测试逐项验证photoAdd按旧canEdit回填。完整结果在该任务result.json与分阶段日志留存。
仍需服务端回归及部署检查。不得提前同步一个依赖新字段的客户端给旧后端。
真机与双账号验收依用户最新指示暂缓，不是自动测试已通过。
