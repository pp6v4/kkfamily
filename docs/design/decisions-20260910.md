# 2026-09-10 范围与交付节奏确认

## R05 微信订阅提醒延期（U）

乙方询问微信订阅模板ID与字段截图，用户回复：“还没选，我觉得先不搞这些吧”。据此暂停微信订阅提醒的模板选择、授权入口、发送开发及联调；保留现有家庭待办与站内Inbox。不将“待办有提醒”整体删除，也不推断已永久取消微信渠道。

当前客户端关闭订阅入口和直接处理函数，不再请求模板设置；保留既有后端回执数据与接口、不删除用户数据。站内消息仍按既有规则工作，界面明确它不等同于微信推送。恢复需用户重新提出需求，再确认模板与平台资格。此项列为DEFERRED_BY_USER，不再作为当前一期阻塞项。

## R06 按功能批次交付（U/E）

用户询问反复上传部署是否浪费token，并指出进度慢。乙方调整：开发中按改动运行针对性检查；完整功能批次结束做全量回归；只在后端有必要变更时部署，客户端在可体验节点统一构建同步。保留必要的鉴权、数据一致性和发布验证，不重复执行与当前改动无关的服务器检查。

本地小程序构建同步与服务器部署是两种操作。Git提交用于保存可追溯变更，不自动触发构建、上传、数据库迁移或发布。新增`pnpm check:client`一次执行lint、类型检查、客户端测试，任一步失败即停止；此命令不构建、不访问生产、不部署。

## 下一批交付范围

优先完成实际使用所需的构建一致性、菜谱/点餐/购物与登录体验验证。已有待办站内提醒保留。COS照片真实写读、微信双账号及地图联调仍待实际环境验收，不因延期订阅而自动视为完成。不自动增减库存、不加入账本预算的决定不变。
