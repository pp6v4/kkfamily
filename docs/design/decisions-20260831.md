# 产品规则确认与操作授权记录

确认日期：2026-08-31。确认来源：本任务中，乙方列出R01～R03建议并请求本地虚构数据预览操作授权后，用户回复“同意，授权”。本记录只确认下列明确事项，不将其扩大为对所有工程细节或正式发布的批准。

## 已确认的产品规则（U）

| 编号 | 原需求与乙方建议 | 本次确认结论 | 影响范围 |
| --- | --- | --- | --- |
| R01 | 每人可选某餐想吃的菜；乙方建议确认前本人撤回，确认后由厨师或餐点管理者改单并保留快照 | 确认后允许厨师或餐点管理者修改菜单，保留变更记录和新快照，不覆盖旧快照 | D05；P03/P08/P09；MealSnapshot及修改审计 |
| R02 | 非行程成员不可访问，历史参与者可查看；乙方建议主动安全撤权优先 | 正常完成行程保留历史参与关系；主动撤销行程访问后停止访问，不因曾参与而恢复 | D07/D10/D13；P15/P23；TripMember、媒体、日历、看板和消息 |
| R03 | 家庭档案和工单；乙方建议先做地址、联系人及自定义非高敏字段，工单作为家庭请求型待办 | 首批档案按上述范围；工单用于家庭事项请求与处理，不默认作为软件问题反馈系统，不主动收集证件等高敏资料 | D11/D12；P29/P30/P33/P34；Task、ArchiveField及字段权限 |

规则已确认不代表对应接口、数据库迁移或真机验收已完成。地址、联系人仍属于个人信息，应按权限保护；“非高敏”不等于公开。

## 本次操作授权

允许在本机本地设计预览页面中，使用虚构示例数据执行点击、切换和原型检查。页面不得连接生产API、写真实数据库、变更真实账号权限、上传真实照片/档案或发送通知。本次不部署生产环境。

## 未被此次确认覆盖的事项

农历生日一期范围仍待单独确认；地图Key、微信订阅模板、真实微信双账号及备案/HTTPS状态仍待联调核实。工程建议仍标E；当前源码事实仍标V。审批本地预览不等于批准上线或认定42条业务验收通过。

## 确认规则的补充验收设计

以下为针对本次决定的补充用例，不改变A01～A42原编号；状态均为NOT_RUN，必须用真实服务环境取得证据后才更新。

| 编号 | 输入与操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| C01 | 某餐已确认；普通点菜者请求改单，再由厨师或餐点管理者修改 | 普通点菜者无权覆盖确认菜单；授权修改生成新快照和审计，旧快照可追溯；缺料比对采用新版本 | NOT_RUN |
| C02 | 成员参加行程并正常完成；之后主动撤销其访问，再查询详情/日历/相册/看板/消息 | 完成后仍可按原权限看历史；撤销后不再返回该行程私有内容，新读图URL拒绝。此前签名URL按原有效期存在短窗口，不承诺立即失效 | NOT_RUN |
| C03 | 创建家庭请求型待办并分配负责人；创建地址、联系人和自定义非高敏字段，以无授权账号查询 | 待办按家庭事项流转而非软件缺陷工作流；无字段授权者看不到字段和值；不默认出现证件等高敏必填项 | NOT_RUN |

## 溯源与版本

本记录解决旧评审稿中R01～R03的待决状态；旧版文件保留作历史依据。设计契约、字段字典、交互说明和新版Word文档同步引用此结论。后续变更须新增决定记录，不能回写本次用户原话。
