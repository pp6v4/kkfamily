# 一期行程地图、路线与住宿决策（2026-09-02）

## 已纳入一期

- 每趟行程保存有序节点，坐标统一标记为GCJ-02。
- 节点可通过微信原生选点或手工填写经纬度；拒绝位置选择不阻断手工录入。
- 路段记录起点、终点和自驾/火车/飞机/步行/其他交通方式。
- 没有供应商规划结果时仅绘制“示意路线”，不填造距离、时长或导航结果。
- 节点编辑或重新排序后，既有路线标记为待重新规划。
- 住宿保存名称、地址、入住/退房日期、联系信息和预订备注，不记录预算或账单金额。
- 删除节点前必须展示关联路线及住宿数量并二次确认；关联路线归档，住宿保留并解除节点关联。
- 待出行路线使用闪烁虚线，旅途中和已完成使用实线箭头；页面隐藏后停止动画定时器。

## 权限和并发

- 只有该趟行程的ACTIVE成员且拥有trips:EDIT和行程编辑权才能修改。
- HISTORY成员可读不可写，REVOKED成员不可进入；家庭管理员未加入该趟行程也不可读取。
- 行程、节点、路段和住宿均提交版本号，过期写入返回冲突并要求刷新。
- 路段两端必须属于同一趟行程，禁止跨行程引用。

## 明确留后

- 腾讯位置服务Key尚未提供，一期当前不接地点搜索和真实道路规划；接口及PLANNED类型保留，供应商、真实几何同时存在时才允许标记规划路线。
- 原生地图线型、箭头和动画需微信真机验证；构建成功不等于真机验收。
- 行程私有照片另行接入已有私有媒体能力，不与本次节点/路线数据混写。
- 做饭自动扣库存、购物自动入库存不属于本功能，也继续维持一期不做的范围决定。
