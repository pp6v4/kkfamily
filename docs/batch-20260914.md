# 2026-09-14 开发批次（未部署）

## 范围

落实[R05/R06](design/decisions-20260910.md)：暂停微信订阅，保留站内提醒，减少碎片化部署。本批不改后端业务、不迁移数据库、不连接生产服务、不构建或同步用户导入目录、不发布微信版本。

- 通知页关闭微信订阅卡片、模板读取和直接调用入口；即使残留已配置模板也不申请授权或发送回执。文案明确提醒位于小程序内，不是微信推送。既有后端接口和数据保留。
- 新增根命令`pnpm check:client`，依次运行lint、vue-tsc、客户端测试；任一步失败即停止，不夹带构建或部署动作。
- API Dockerfile将`--no-frozen-lockfile`改为`--frozen-lockfile`，以后依赖声明和锁文件不一致必须失败。依据[pnpm安装文档](https://pnpm.io/10.x/cli/install#--frozen-lockfile)，不在构建时重新解析版本。

## 已执行验证

- `pnpm check:client`完整执行成功：lint0错误0警告、vue-tsc通过、248/248客户端测试。测试总数未增加，原订阅回执测试替换为当前“延期且站内可用”的产品规则测试。
- 重点用例确认：通知页仍加载消息和偏好；模板请求0、微信授权请求0、回执写入0；直接处理函数无法绕过延期；开关与已读页生命周期用例继续通过。
- 使用Corepack定向运行pnpm **10.12.1**，在新建临时副本中对4个workspace执行`install --lockfile-only --frozen-lockfile --ignore-scripts --offline`：退出0，锁文件SHA256不变。
- 仅将副本的`@nestjs/common`声明改为虚构不匹配版本`^999.0.0`后执行同一命令，得到`ERR_PNPM_OUTDATED_LOCKFILE`，确认失败策略生效；真实manifest与锁文件未受该反向测试影响。
- 临时验证目录`.codex-tmp/lockcheck-96043b604bf94be2963ff60e4e9a01e7`，未删除原依赖目录或用户NUL文件；没有在Windows重新安装业务依赖。

## 未被上述证据证明的事项

锁文件元数据检查不是从空缓存安装成功，也不是Linux完整镜像构建。本机没有Docker命令，本批没有为这一行构建配置变更重复上服务器打包；下一次后端发布前仍须验证严格锁定的镜像构建。旧版本已部署记录见[9月10日部署](deployment-20260910.md)，本批不能冒充线上已更新。

uni-app peers兼容、真实微信双账号、COS媒体、地图等门槛仍在；微信订阅已由用户延期，不再要求用户当前提供模板。后续优先完成用户可体验的一整批业务验证与改进，再统一同步小程序。
