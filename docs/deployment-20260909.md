# 2026-09-09 部署记录

## 最新后端更新（北京时间22:04）

住宿日期修复版本 `40dde83` 已上线；正式运行镜像为 `kkfamily-api:40dde83`，SHA256 `f33e2e8073818fb4ddbb8b512264a9faf285d791a25ac01df70fc83c3e43e15a`。API和提醒worker均运行该镜像，部署后复查运行约1分钟、重启次数0，API为healthy。此次没有数据库结构变更，18批迁移校验和一致、无待执行迁移、结构差异为零。

- 部署前：隔离PostgreSQL的旧数据回归6/6、通知6/6、住宿5/5和HTTP51/51通过；真实runtime镜像启动、18批迁移、未登录访问401及worker启动通过。
- 部署时：核对当前旧镜像必须为下方首次升级的3d0d2288…摘要，且所有已应用迁移校验和一致；重新备份数据库和配置，保留旧镜像，再停止API/worker并切换。
- 部署后：HTTPS首页200、health200/status=ok、未登录recipes401，TLS主机名及证书链验证通过。实际运行容器内的5条住宿单元测试通过；该单元测试使用模拟数据库，不对生产数据做测试写入。生产接口的真实双账号业务验收仍待完成。
- 新备份目录：`/home/ubuntu/kkfamily-release-20260909T140318Z-f1f01e51/`，私有权限700；`production.dump` SHA256 `bc93f420284c6f7a462a09bcc37342f5a4b196d0d7219d8c0cd2e01fb6aa7cc7`。私有源码/配置备份不公开分享。
- 回退镜像：`kkfamily-api:rollback-20260909T140318Z-f1f01e51`。本轮未新增迁移，但回退仍需评估切换后数据，不自动恢复旧数据库备份。
- 执行证据：`.codex-tmp/production-release/20260909T140318Z-f1f01e51/release.json`；隔离快照20260909T091556Z-c894f306，runtime冒烟报告runtime-smoke-6c437564ecb002a9.json。
- 最新小程序产物已在此前原生交互修复中更新至77个文件、客户端105/105；本轮只改后端，无需重新构建小程序。正式发布、真实COS/微信/地图/订阅消息验收条件没有因此自动满足。

## 首次后端升级记录（北京时间06:46，历史快照）

后端版本 `9c94cdd` 已部署至 `49.233.18.181`。北京时间 06:46 完成生产迁移与 API、提醒 worker 启动；约 5 小时后复查均运行正常、重启次数为 0。此为后端升级完成，不等于小程序正式发布或全部外部服务验收完成。

- 生产数据库：从 3 批升级到 18 批迁移，Prisma 结构差异为零；没有重置数据库或删除生产数据卷。
- 运行镜像：`kkfamily-api:9c94cdd`，API 与 worker 使用相同镜像。
- runtime 镜像 SHA256：`3d0d2288e8b81dbe1d93843b13852e5e9a0f72c48852cc0a8ed3e45b87073c46`。不是隔离数据库测试的 build-stage 镜像。
- HTTPS 首页与 `/api/v1/health` 返回 200，健康状态 ok；未登录访问菜谱返回 401。HTTP 跳转 HTTPS；全部业务请求仍通过同域名 `/api/` 反向代理。
- 数据库、Redis、API 没有宿主机公开端口；仅 Nginx 提供 80/443。证书自动续期已启用并通过演练。
- 小程序源码改为直接请求 `https://pp6v4.com/api/v1`，避免依赖 HTTP 跳转。客户端测试 56/56 通过，包含 JSON POST 和二进制 PUT 的 HTTPS 地址、方法及请求体校验。
- 微信生产构建成功；75 个构建文件同步到项目上级 `outputs/mp-weixin`，逐文件 SHA256 不一致数 0；保留开发者工具的 `project.private.config.json`。产物包含 HTTPS API 地址，不再包含旧 HTTP API 地址。

## 上线门禁与备份

1. 隔离 PostgreSQL：18 批迁移、零结构差异、旧数据回归 6/6（父测试及 5 个场景）、通知测试 6/6、HTTP 集成 50/50。
2. 独立 production-mode runtime 冒烟：使用临时数据库和非生产测试凭证，验证镜像默认启动、迁移、健康接口、未登录权限保护与 worker 启动。
3. 部署前核对 149 个业务/构建源文件与测试快照哈希，以及生产已应用的 3 批迁移校验和，确认无失败迁移。
4. 新取生产备份和源码/配置备份后切换。旧 API 停止至新服务验证通过约 17 秒，此为执行阶段耗时，非监控测得的精确用户中断时间。

备份目录（服务器私有权限 700）：
`/home/ubuntu/kkfamily-release-20260908T224523Z-96713e5d/`

- `production.dump`：SHA256 `4631311299122e0b8cf7464cf335237793c2eb1be04b7ddecf6080ce27412d37`。
- `source-and-config-before.tar.gz`：含私有配置，权限 600，不下载、不提交仓库或公开分享。
- 旧镜像保留为 `kkfamily-api:rollback-20260908T224523Z-96713e5d`。数据库已经升级，不能假定仅换旧镜像即可安全回退；回退前须评估结构兼容性和部署后的新增数据，不自动覆盖数据库。
- 本地脱敏执行记录：`.codex-tmp/production-release/20260908T224523Z-96713e5d/release.json`。

## 尚待验收

- 微信 AppID/AppSecret、JWT 已配置；真实微信登录、双账号授权和真机流程尚未验收。
- 档案加密密钥已加载至运行容器，并有服务器内私有恢复副本；仍需服务器外的安全备份及轮换演练。
- 媒体驱动为 disabled：缺少 COS SecretId/SecretKey，真实照片上传下载尚未验收，不能当作已可用。
- 微信订阅模板尚未配置；不能把站内提醒 worker 正常运行等同微信推送已完成。
- 地图供应商搜索、真实道路规划及真机动画尚未完成。
- 首页已有 `辽ICP备2026020161号-1` 工信部链接；小程序平台备案状态和公安备案仍待确认。

无需为本次客户端 HTTPS 地址修改再次重启后端。微信开发者工具继续导入原 `outputs/mp-weixin` 目录，重新编译后进行验收。
