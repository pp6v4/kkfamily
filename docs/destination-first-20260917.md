# 先选目的地再创建行程（待部署）

**2026-09-18更新：** 用户授权后已修复单文件读取权限；代码推送、隔离数据库两组53/53、运行镜像验证、生产部署和原小程序目录同步均已完成，见[部署记录](deployment-20260918.md)。下文“待部署/连接失败”保留为9月17日历史状态，不代表当前状态。真机和其余首发门槛仍待验收。

## 本批实现

- 露营地图下增加选目的地入口。使用原生选择器，保留原行程名和日期；空名称用地点名填充。取消保留原草稿。
- 处理选择器回调与onShow先后两种顺序；页面隐藏清除可见私有内容，返回后重新校验家庭、账号和编辑权限。切换身份、撤权或卸载页面不恢复旧选择。选择过程中禁止保存。
- POST /trips新增可选initialDestination对象（title/latitude/longitude）。旧客户端不传仍兼容；null、数组、缺字段、越界、字符串坐标、额外字段拒绝。
- 创建时在Serializable事务内检查权限，以嵌套创建写入初始GCJ02营地节点，并创建日历事件。不分两次接口保存，不推测抵达时间，不创建虚构路线。不新增数据库迁移。

## 验证与边界

- 后端编译、lint通过；trip-create.unit.test.cjs 3/3。测试核对输入校验、零坐标、小数、事务内权限、嵌套节点与日历调用、异常传播和拒绝写入。单元测试使用事务替身，不宣称已验证真实数据库回滚。
- 客户端check:client 252/252，lint/vue-tsc通过；新增选点回调顺序、单次创建坐标载荷、取消、身份切换、撤权和卸载用例。微信构建输出DONE。
- 隔离数据库集成新增用例已写入access.integration.test.cjs，并将单元测试加入run-isolated.cjs；本批尚未执行数据库集成。
- 服务器Paramiko导入因本机`.codex-tmp/paramiko/paramiko/__init__.py`读取权限被拒而失败；GitHub工具的依赖也有同类问题。本机没有Docker。未擅自更改目录ACL或绕过权限。
- 未同步outputs/mp-weixin、未推送成功、未部署后端、未发布微信。用户原测试目录仍是上批地图首屏版本，新版仅在源码及客户端dist构建目录。

恢复连接后：先跑隔离数据库完整回归并核对持久化结果，完成运行镜像验证，再统一部署后端与同步客户端；真机验证选点、保存及返回总览标记。COS、配偶共享家庭和道路规划仍沿用首发未完成项。
