# 首版发布就绪核对（2026-09-22）

本表区分工程交付与微信正式发布。不得因服务部署成功而认定平台审核已完成。

| 检查项 | 本次实际证据 | 状态 |
| --- | --- | --- |
| 后端 | 生产a65e884，19项迁移，API/worker摘要一致且重启0；详见deployment-20260922.md | 通过 |
| 客户端 | 1a75dd4，lint/类型检查/257项测试通过；78个构建文件逐个SHA256一致，保留1个工具私有配置 | 通过 |
| AppID | manifest、构建project.config.json均为wxd7ff83eda505dd31 | 一致 |
| 网络 | 源码API_BASE_URL为https://pp6v4.com/api/v1；构建配置及本地私有配置urlCheck均true | 未关闭合法域名检查 |
| 页面 | 构建app.json共14页面，5个底部入口，最后一个为“我的家” | 通过 |
| 位置声明 | manifest和构建app.json均声明chooseLocation及露营选点用途 | 代码声明存在；平台准入与真机授权未验收 |
| 产物体积 | 原导入目录79文件、425485字节，包含工具私有配置；不是平台上传包压缩体积 | 已测量 |
| 产物敏感文件 | 扫描.env、常见私钥/证书、数据库备份、压缩包文件名，命中0；常见私钥/AKID/sk格式内容标记命中0 | 有限规则扫描通过，非全面安全审计 |
| 容器构建边界 | .dockerignore排除.env、私钥、.ssh、work、outputs；Dockerfile复制业务目录及依赖 | 源码规则已核对，不替代镜像逐层取证 |
| ICP展示 | “我的家”展示辽ICP备2026020161号-1并支持复制 | 已实现；不要将网站ICP号等同小程序备案号 |
| 隐私说明 | 已补“我的家→隐私保护指引”，无需家庭权限，调用原生openPrivacyContract；不支持、失败明确提示，不写入同意状态；258项客户端测试通过 | 入口实现；公众平台实际声明及真机打开仍待核实，不能宣称已满足平台发布要求 |
| 真实账号协作 | 生产只有1个有效管理员，业务记录仍为空；用户当前暂缓真机验证 | 待验收 |
| 微信正式发布 | 尚无已上传版本、审核通过或发布回执证据 | 未完成 |

## 范围冻结

### 部署暴露面补充实测

2026-09-22通过Paramiko执行只读检查：5个生产容器均运行；API、worker、PostgreSQL、Redis均无宿主机端口映射，仅Nginx映射80/443。PostgreSQL和Redis各仅连接一个Internal=true网络。
实际API镜像摘要与a65e884发布一致；镜像固化环境变量名称仅PATH、NODE_VERSION、YARN_VERSION、NODE_ENV，无SECRET/TOKEN/PASSWORD/DATABASE_URL/PRIVATE_KEY名称。
以无网络、只读、去能力的临时容器检查最终镜像/workspace应用目录484个文件，常见.env/私钥/证书/数据库dump文件名命中0。排除了node_modules，未逐层扫描历史镜像，也未作全内容秘密识别，因此仍不将该检查称为全面安全审计。检查未修改生产服务或数据库。

自动库存扣增、账本预算、微信订阅消息继续延期；路线保留已明确的示意线路，不声称已有道路导航。
不为填满测试表增加演示生产数据、不代替用户确认实际体验、不自动提交未验收版本。

## 下一步门槛

隐私入口通过当前uni-app类型声明和微信适配器调用链核对，并测试成功调用、缺少API、失败回调、同步异常、离页忽略回调。官方文档页面本轮工具读取失败，未以第三方博客作为平台规则证据。平台侧实际配置必须取得真实证据，不用源码字段代替公众平台状态。
用户恢复验证时，仅按最短流程检查本人登录、家人加入、菜谱点餐购物、露营邀请及照片。随后再进行微信上传、审核、发布，并分别记录结果。
