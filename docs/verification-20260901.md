# 实现验证记录（2026-09-01）

本记录只陈述实际执行的验证，不把尚未实现或尚未真机联调的功能标为通过。

## 隔离环境

- 运行位置：云服务器上的独立 Docker 网络、独立 PostgreSQL 测试容器及测试 schema。
- 生产 `/opt/family-life`、生产 PostgreSQL、Redis 与线上容器未执行迁移、重启或业务写入。
- 身份数据均由测试生成，微信登录未调用真实微信接口。

## 已执行结果

| 层级 | 结果 | 覆盖边界 |
| --- | --- | --- |
| 全新数据库迁移 | 8/8 migration 成功 | 从空 schema 依次应用初始、日历、行李模板、成员权限、餐点快照、菜谱版本、私有媒体、行程协作迁移 |
| Nest HTTP + PostgreSQL | 34/34 通过 | 家庭隔离、授权、邀请并发、管理员保护、日历来源权限、菜谱编辑冲突、私有图片、多人点餐、菜单快照、库存辅助判断、购物幂等与复购、行程成员/历史/撤权/小组/责任/版本 |
| 媒体字节单元测试 | 1/1 通过 | JPG/PNG/WebP签名字节、声明类型及实际大小不一致拒绝 |
| 客户端逻辑/组件 | 12/12 通过 | 加入家庭、权限刷新、成员编辑冲突、日历跳转、确认弹窗取消、完成餐点无库存负载、复购重试、购物版本、行李版本及同域媒体请求 |
| API TypeScript 构建 | 通过 | Nest 编译和 Prisma Client 生成 |
| 微信小程序构建 | 通过 | uni-app `build -p mp-weixin` 生成构建产物 |

关键范围断言：完成餐点重复提交保持幂等，且库存数量不变、没有自动消耗流水；购物项标记已购买和复购均不改变库存。缺料项由服务端按已确认菜单快照及当前人工库存重新计算，客户端不能自行提交任意数量。菜谱编辑使用版本号防止覆盖他人修改，草稿详情不向只有查看权的账号暴露；已确认餐单继续读取冻结快照，不随菜谱后续修改漂移。

私有图片关键断言：无图草稿可保存但发布被拒；客户端得不到COS objectKey；上传申请绑定家庭、成员、菜谱及version；伪造格式、大小、Content-Type和checksum被拒；重复确认返回同一资产；撤权后不能生成新读链接，已生成能力链接保留60秒窗口。首次执行发现路径参数承载JWT触发414，改成查询参数后在原测试schema和全新schema均完整通过。

2026-09-02行程协作关键断言：家庭管理员未加入行程时仍不能读取；加入后可见，REVOKED后列表、详情和日历立即不可见；正常完成将ACTIVE成员转为HISTORY并保留只读访问；最后一名OWNER不能撤销或降级；准备小组拒绝非行程成员和过期版本；撤销仍负责未准备物品的成员会被拒，只有明确清空责任后才成功；行李项按版本更新，软排除后默认列表不再返回，历史数据库行及来源名称快照保留。原测试schema与全新schema均为35/35总测试通过（34个HTTP/PostgreSQL集成测试加1个媒体单元测试）。

2026-09-02行程路线、相册及家庭待办增量：Prisma Client生成、API TypeScript构建和微信小程序构建通过；客户端测试15/15、媒体字节单元测试1/1通过。新增6个HTTP/PostgreSQL测试覆盖节点/路线/住宿版本、路线过期、过期规划路线不可借普通编辑恢复、删除影响确认、跨行程节点拒绝、历史成员只读、行程照片成员访问/撤权/完成后补传，以及待办分配/版本/处理记录/带原因重开/取消后日历移除，但本轮未成功连接云端隔离数据库执行：当前Codex进程无法读取本机已绑定SSH私钥（ACL拒绝），本机也没有Docker。因此10个迁移的全新数据库验证和40个HTTP/PostgreSQL测试仍为待执行，不能提前登记为通过。

2026-09-03收藏与灵感增量：新增第11个迁移，API TypeScript构建、Prisma schema校验和微信小程序构建通过；客户端逻辑测试16/16、媒体字节单元测试1/1通过。新增HTTP/PostgreSQL测试覆盖个人/家庭可见性、管理员查看、链接协议、版本冲突、收藏私有图片、转换并发幂等、空菜谱草稿禁止发布、来源保留、待办转换及归档不级联删除。该测试目前只是测试声明，因同一SSH/本地Docker条件仍未在独立PostgreSQL执行；当前共41个HTTP/PostgreSQL测试待云端回归。

2026-09-03家庭档案增量：新增第12个迁移，API TypeScript构建、Prisma schema校验和微信小程序构建通过；客户端逻辑测试17/17、媒体字节单元测试1/1通过。新增HTTP/PostgreSQL测试覆盖管理员/成员/指定成员字段ACL、跨家庭授权拒绝、数据库密文、显式读取、独立值版本、无效日期、字段版本、归档隐藏和审计无明文。该测试尚未在独立PostgreSQL执行；当前共42个HTTP/PostgreSQL测试待云端回归。生产档案读写还必须配置并安全备份32字节AES密钥，不能使用测试密钥。

## 尚不能据此认定通过

真实微信双账号登录、微信开发者工具交互、原生地图真机效果、地图供应商搜索/真实道路规划、真机网络、真实COS写入/读取和批量相册、收藏真机选图、档案生产密钥/真机读写、生产迁移兼容检查、生产部署和用户验收均未在本记录中执行。待办提醒的Inbox/outbox/worker、看板及通知仍未实现；项目不是正式发布版。
