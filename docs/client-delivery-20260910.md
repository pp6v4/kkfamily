# 2026-09-10 小程序客户端交付记录

## 更新接口兼容与完整类型检查（北京时间17:19）

- 实现`ad83430`：逻辑PATCH通过POST `/_patch`兼容入口保存；保持HTTPS同域名`/api/`，保留原PATCH后端路由；完整vue-tsc通过，客户端248/248、lint0错误0警告，构建成功。
- 配套后端已先部署；隔离数据库原/兼容HTTP各51/51，正式镜像兼容2/2、生产Nginx24入口全部未登录401。之后同步78文件至原目录，SHA256差异0，私有配置未覆盖。
- 未上传/发布微信小程序，仍不是完成真机及外部服务验收的正式版本。详见[部署记录](deployment-20260910.md)和[兼容修复记录](patch-compatibility-20260910.md)。下方较早记录保留历史时间与验证边界。

## 客户端静态检查门槛（北京时间11:53）

- 实现提交`faa8baf`。固定受支持的ESLint 10与Vue/TypeScript解析器，检查全部28个源码，修复发现的问题；lint为0错误0警告，规则合同7/7，全量241/241。
- 冻结离线安装及微信生产构建均通过；78文件同步原目录、SHA256差异0、私有配置保留，API地址不变。没有服务器变更和平台上传/发布。
- 已有uni-app peer版本警告、pnpm声明与实际版本差异、完整类型检查和真实联调仍待验证，详见[质量检查记录](client-quality-checks.md)。

## 登录与加入家庭页面（北京时间11:40）

- 实现提交`4420aa7`。页面读取/写入绑定视图与会话代次，邀请码/昵称/家庭列表离页清空，旧退出确认失效，返回时未完成写入继续锁定；进入家庭前重新核实成员资格和访问令牌。
- 新增13条Vue组件/模拟会话回归，全量234/234通过，微信生产构建成功；78个文件同步原目录、SHA256差异0、私有项目配置保留，API地址不变。
- 成功创建/兑换/选择家庭后锁住再次提交；跳转失败明确提示家庭已选定，用户可以重新读取账号。退出后显示明确登录按钮，不因页面再次onShow自动恢复登录；远端注销失败不会冒充全部撤销成功。
- 无服务器、数据库、真实家庭成员、微信平台上传/发布变更。仍需真实双账号和真机验收；下一步补齐客户端lint工具与规则并继续按验收条目收口。

## 公共会话与API续期保护（北京时间11:30）

- 实现提交`157d00f`。退出/选家庭使旧登录、权限请求、令牌续期及业务请求失效；普通JSON、图片二进制和成员API均检查同一会话代次。旧权限不回滚较新版本，权限响应不覆盖已轮换令牌。
- 专项初始10项在旧代码上2通过8失败；修复后专项19/19、全量221/221通过。专项使用真实session/API模块搭配模拟传输和微信登录，不是只模拟会话结果，也不等同真实微信账号登录验收。
- 微信生产构建成功，78文件同步原目录，SHA256差异0，私有项目配置保留；产物确认两套API均含assertSessionEpoch，API基地址不变。
- 本轮未改服务器/数据库，未发布小程序。退出立即清本地会话；远端撤销若断网仍会报告失败，不宣称已完成服务端撤销。已经发出的业务写入不能被本地代次取消，返回页面仍需核实服务端状态。

## 成员管理与邀请码保护（北京时间11:21）

- 实现提交`4c5d310`。成员/邀请读取、分页、编辑和确认框绑定当前家庭/成员、页面代次及原始版本；保存后重新核实权限，权限被收回后不保留管理员表单或邀请码。
- 新增17条成员Vue组件回归和2条成员API参数/401续期回归，全量202/202通过；模拟API证据不等于生产管理员权限变更验收。
- 最终微信生产构建成功；78文件同步原导入目录，SHA256差异0、私有项目配置保留，API仍为`https://pp6v4.com/api/v1`。初次产物检查把动态提示误判为WXML静态文本：核对编译器输出后，改为检查JS动态文本和WXML绑定控件，检查通过；不是绕过构建错误。
- 本轮没有修改后端、数据库或生产服务；没有微信上传/发布。邀请码离页清空仅针对页面内存，不会擅自清除用户系统剪贴板。

## 购物与手动库存保护（北京时间05:48）

- 实现提交`3e4fbb3`。购物/库存按家庭与成员、页面读取代次隔离；页面隐藏/卸载清空私有列表和草稿；保存锁跨隐藏保留，旧写入结束后要求返回页面重新确认数据。
- 新增16条真实Vue setup、模拟网络的组件测试，全量183/183通过。覆盖购物三类录入、数量边界、版本更新、复购幂等键重试、手动库存0/未知/调料语义、日期清空、409草稿保留、401/403清理及迟到请求。此证据不等于真实双账号、数据库或真机整链验收。
- 最终微信生产构建成功；78个文件同步到下节同一原导入目录，逐文件SHA256不一致0、私有项目配置不变。产物含“离开前的操作已结束”保护、“最多3位小数”校验和不自动增减库存提示；API地址仍为`https://pp6v4.com/api/v1`。
- 无后端、数据库及线上服务变更，无微信平台上传/发布。到期日清空沿用现有后端完整替换语义，不需要迁移；库存位置不能修改，界面补充说明。

## 点餐请求与确认保护（北京时间05:34）

- 实现提交`f8e42ae`。点餐请求按身份/餐点/日期绑定，旧确认框失效，重复写入锁定，加餐未提交名称不再混用当前餐点；完成用餐不自动改变库存。
- 全量客户端测试167/167，新增22条；最终微信生产构建成功。模拟API测试不作为真实双账号验收结果。
- 78个文件同步至下节同一原导入目录，逐文件SHA256差异0，`project.private.config.json`未改变。编译产物已检查新加餐确认提示及账号变化保护，API地址仍为`https://pp6v4.com/api/v1`。
- 未操作微信平台上传/发布，未改后端或数据库，也未重启生产服务。

## 菜谱编辑与封面修复（北京时间05:16）

- 实现提交：`71ee3dc`。修改菜谱编辑页权限与生命周期、保存锁、输入校验，以及原生封面选择返回后的草稿/身份/版本保护。
- `pnpm --filter @family-life/client test`：145/145通过，其中本轮新增21条。测试模拟微信选择器与网络，不代表真实COS写读或真机验收。
- `pnpm --filter @family-life/client build:mp-weixin`：成功。初次构建后又补充目标菜谱切换和异常文件返回保护，最终构建再次成功；同步的是最终产物。
- 78个构建文件同步至`C:\Users\10353\Documents\Codex\2026-08-26\new-chat\outputs\mp-weixin`；逐文件SHA256不一致数0；`project.private.config.json`校验一致、未覆盖。
- 已检查编译后的菜谱脚本包含新权限和数量校验；API地址仍为`https://pp6v4.com/api/v1`。微信开发者工具继续对原目录重新编译。
- 未执行微信平台上传/发布。本轮未改后端、未重启服务、未执行迁移、未写生产业务测试数据；后端部署沿用[9月9日记录](deployment-20260909.md)中的`40dde83`，不是把客户端提交误当作线上API版本。

## 保留的验证边界

客户端lint、完整类型检查已完成并通过；全量248/248。依赖兼容、声明包管理器客户端复现以及真实微信双账号/COS/地图/订阅消息和正式验收仍未完成，详见[质量检查记录](client-quality-checks.md)和[需求差异记录](implementation-gap-audit.md)。
