# 代码与设计差异核对（2026-09-10）

当前核对截至2026-09-10工作区。本文件区分“源码已实现”“本地已验证”“真实环境已验收”三种状态。存在页面、接口或测试声明，不等于已经在真实微信账号和生产数据库中验收。

## 总结

9月10日17:06后续：完整类型检查已通过；发现微信更新操作PATCH不在平台支持契约内，已增加POST兼容入口并完成客户端248/248、全Controller路由兼容测试2/2。隔离数据库双传输回归及配套部署尚待执行，新客户端未替换原导入目录。当前证据以[兼容检查记录](patch-compatibility-20260910.md)为准，下文较早的“类型检查未完成”是历史状态。

一期主业务已经从页面原型推进到数据库 API：共享家庭和权限、菜谱、多人点餐、库存辅助比对、购物清单、露营协作、待办、收藏、档案、看板、站内提醒均有服务端模型和客户端入口。做饭完成不自动减库存、购物完成不自动加库存，是 2026-09-01 已确认的一期规则，不是缺陷。

## 登录、加入家庭与退出页面（2026-09-10 11:40，客户端234/234）

对应U01/U02、D02/D03、A01～A08。实现提交`4420aa7`，仅改客户端和测试。

- 页面绑定视图与公共会话代次，较早登录成功/失败不能覆盖新读取；离页、卸载或会话替换清除身份、家庭列表、昵称和邀请码。直接处理函数亦检查当前可操作身份，输入及重复提交在写入期间锁定。
- 创建名称裁剪后1～40字符；兑换必须完整32位字母/数字/下划线/连字符，昵称1～30字符。创建和兑换是两个明确端点，选择返回的家庭/成员及角色，不根据微信昵称自动授权。错误保持当前有效草稿，401/403清理私有内容。
- 保存结果在离页后不调用rememberSession或导航；返回时仍等待原写入结束，再提示重新读取账号确认实际结果。已经发送的HTTP请求仍可能在后端成功，本地不能撤回事务，也不自动重新创建家庭。
- 已有家庭通过稳定membershipId选择而非数组下标，进入前重新取得微信账号资料、确认同一用户及ACTIVE成员资格，使用新访问令牌，不将页面缓存的旧令牌写回会话。选择成功后锁住重复操作；跳转失败明确“家庭已选定”。
- 退出弹窗绑定原页面、会话和确认代次，重新登录/选择家庭/另开弹窗/离页后失效。确认后立即清除页面身份，等待服务端撤销时继续锁定，远端失败明确只完成本机退出。主动退出后onShow不自动重新登录，需要点击登录按钮。
- 新增13条组件回归，全量234/234，构建成功，78个交付文件SHA256一致且私有配置未覆盖。测试模拟微信/网络，没有执行生产创建家庭或兑换邀请码，不把此结果作为真实双账号验收。

本节时点发现的客户端lint缺口已在[质量检查记录](client-quality-checks.md)补齐，28个源码全覆盖且0错误0警告；完整类型检查、依赖兼容及真实验收仍未关闭。

当前仍不能称为正式版本。9月9日最新隔离PostgreSQL17验收执行18批迁移，结构差异为零，通知单元测试6/6、住宿5/5、HTTP集成测试51/51通过。随后生产备份与后端升级部署已完成，生产18批迁移、API与worker均通过复查；客户端HTTPS构建已同步。9月10日客户端回归241/241通过，不代表所有业务已真机验收。剩余工作包括依赖兼容和类型检查、微信/地图/COS真实联调和真机验收。网站ICP服务备案号和HTTPS已上线；小程序平台备案、公安备案等仍待确认。详见[后端部署记录](deployment-20260909.md)和[最新客户端交付记录](client-delivery-20260910.md)。

## 公共会话与API续期闭环（2026-09-10 11:30，客户端221/221）

对应U01/U02、D02/D03、A01～A08。实现提交`157d00f`，只改客户端共享逻辑与测试。

- 使用真实session模块和模拟网络复现10项：旧代码2通过8失败，包含退出后权限响应恢复存储、换家庭后被旧结果覆盖、旧promise清理导致第三次登录请求、旧权限版本及访问令牌回滚。不是把静态源码推断当作已复现。
- 公共会话代次在clearSession及显式rememberSession家庭选择时递增，并清除三种pending引用；旧异步流程完成前检查代次。pending只清理自身引用，避免旧finally破坏新登录或续期的单飞状态。微信code晚到时不再发起已失效的登录HTTP请求。
- 权限读取按发起代次接受最新结果，较旧响应及其403不覆盖/移除新授权。已存的较高permissionVersion不被降级，写权限响应保留存储中的新访问令牌。旧请求会明确报告已失效，调用方需使用最新刷新结果。
- 普通JSON、图片二进制、成员管理API在获取会话、收到结果、401续期及重试前后统一检查会话代次。迟到401不在退出或新家庭下续期重试；同一失效令牌的延迟401复用已完成轮换结果。实际请求至多续期重试一次，403/409不作为令牌过期重试。
- 续期严格要求原家庭，原家庭失效时不自动选择另一家庭。成员ID或身份级重试的用户ID变化会停止操作并清会话，不把原请求在另一用户下重放。refreshAccess在401重试后收到403也清家庭上下文。
- 退出先清本地，再尝试远端注销；晚到注销结果不清除后来建立的新会话。网络失败不宣称远端撤销成功，已发送业务请求也可能在服务器完成，不能把本地取消理解为事务撤回。
- 专项19/19，全量221/221；专项组合真实session/family-api/members-api模块和模拟传输，既有只测序列化的测试使用固定代次桩。生产构建和78文件同步校验通过，未做真实微信登录或生产权限变更。

本节时点发现的登录/加入家庭页面缺口已在上方“登录、加入家庭与退出页面”处理并补测；真实微信双账号及真机验收仍未完成。

## 后续成员授权与邀请闭环（2026-09-10 11:21，客户端202/202）

对应U01/U02、D02/D03、A01～A08。实现提交`4c5d310`，只改客户端和测试；本轮没有执行真实管理员转让或停用生产成员。

- 修改前源码的load/more直接回填成员及邀请，只有onShow；newCode和权限草稿无离页清理，旧转让回调使用点击时的session.version。此次组件读取按页面与身份代次隔离，隐藏/卸载清空私有数据，分页单飞、按id合并并防止较低版本覆盖及过期分页插入。
- 权限刷新后才读成员。非members:MANAGE不请求角色目录/邀请码；界面和处理函数均检查权限。保存时深复制角色及覆盖规则，捕获成员版本，409保留原草稿，401/403清空。权限预览与后端一致：多角色取高、显式ALLOW可降级，DENY最后执行，避免重复规则使预览误显示开放。
- 停用/启用、转让、撤销邀请的弹窗捕获目标版本、操作者版本、页面/编辑/弹窗代次。换账号、目标版本变化、另开弹窗、编辑或离页后不再提交；保存期间禁改表单和重复请求。管理员转让提交原操作者version与目标targetVersion，服务端仍负责最后一名管理员、越权授予及事务一致性保护，客户端不冒充这些校验的最终防线。
- 保存成功后清除旧管理内容并重新读取授权和列表，失去管理权限时不恢复旧表单/邀请码。已写入但刷新失败明确提示成功与刷新失败的区别，阻止盲目重提；离页前写入在返回后完成会使返回时的旧读取失效，要求重新确认。
- 家人、点菜访客、露营邀请的角色和显式授予保持原合同；API发送maxUses=1、服务端默认48小时。邀请码只在创建成功且权限复核后显示，刷新/隐藏/卸载清空，不持久化，不主动擦除用户剪贴板。邀请码若已创建却未收到结果，仍需通过邀请列表核对/撤销，不宣称创建请求有跨进程全局幂等。
- 新增17条组件测试和2条成员API序列化/续期测试；全量202/202通过，构建与78文件同步校验通过。单元测试中的转让/停用调用由模拟API响应，不证明真实数据库权限变化或真机验收。

本节时点发现的公共会话风险已在上方“公共会话与API续期闭环”复现、修复和补测。登录/加入家庭页面及真实微信双账号仍待验证，因此未宣称账号整链已闭环。

## 后续购物与手动库存闭环（2026-09-10 05:48，客户端183/183）

对应U04/U05/U14、D05/D06、A13～A20/A37/A46。实现提交`3e4fbb3`，只改客户端与测试，不引入自动库存增减。

- 从修改前源码确认：购物/库存只挂onShow，读取结果直接填入共享数组；保存没有当前家庭/成员核验，loading同时承担读取与写入锁；库存编辑日期使用UTC字符串切片。这里记录的是源码缺口核对，不宣称本轮执行过旧版本红测或生产故障复现。
- 页面读取按代次、家庭/成员隔离，较早成功/失败不覆盖新加载；隐藏/卸载及身份变化清空列表、编辑目标和草稿。无模块权限不读该模块。401/403清空私有数据；加载失败与空列表明确区分。
- 写操作独立单飞锁，捕获请求字段与记录版本，隐藏后仍保持锁至原请求结束。返回时若先读到了旧数据，原写结束会作废该次读取并要求重新刷新，防止误重提。保存成功但列表刷新失败也显示“已保存”，阻止重复写入直至刷新。
- 购物名称1～80、单位至多12字符；数量可空，填写时0.001～999999999.999、最多3位小数。库存名称1～60、食材单位必填；食材数量可0或空，调料不发送数量/单位。显示区区分“有但数量待确认”和数值0；购买状态变化不调用库存写入API。
- 复购只允许当前版本的已购买项，保留原购买记录；不确定结果重试继续使用同家庭/成员/物品的requestId，其他身份不共享。普通新增请求没有服务端幂等键，本轮的并发点击保护不等于跨进程/丢失应答场景的全局幂等。
- 库存编辑按上海日期展示到期日；提交严格校验有效日历日期，保存到当天23:59:59+08:00。核对`InventoryService.set`确认这是完整库存状态替换：未发送expiresAt会清为null，未填数量按availability处理。位置、食材、单位不可在编辑中变更是现有服务端合同，不是清空字段缺陷；界面说明不同位置需另建记录。本轮没有后端迁移或历史库存改写。
- 新增16条真实Vue setup/模拟API回归，全量183/183通过；最终微信构建成功，78个文件同步原目录、SHA256差异0、私有配置保留。组件级测试不会证明生产真实库存更新或微信真机端到端闭环。

本节时点发现的成员管理缺口已在上方“后续成员授权与邀请闭环”处理并补测；公共会话存储与实际双账号行为仍需继续验证，未以购物或成员组件测试冒充账号整链验收。

## 后续点餐客户端闭环（2026-09-10 05:34，客户端167/167）

对应U04/U05、D02/D05/D06、A08/A11～A20/A37/A46。实现提交`f8e42ae`，本轮只改客户端与测试，不更改后端、数据库或库存业务规则。

- 旧源码实测复现：较早列表覆盖新结果，切餐别时旧比对结果仍可能回填，旧完成/取消弹窗在换日期后仍发送请求，连续确认发送两次，以及缺少onHide/onUnload清理。首批10条在旧代码上1通过、9失败，按各测试的具体失败原因记录，不把缺少生命周期函数误说成数据库故障。
- 页面读取、餐点读取和历史读取分别有请求代次；按家庭/成员、日期、餐别、加餐键及明确餐点ID绑定。较早的成功、失败、封面URL和loading结束事件不能覆盖新读取。权限刷新和隐藏/卸载清空家庭数据、库存对照、快照和表单；同一身份返回可用最小餐点标识重新读取，其他家庭不继承。
- 加餐名称拆分为未提交输入和已应用选择。输入修改后明确提示先查询，阻止直接点菜、完成或导入缺料作用在旧餐点。日期/餐别查询支持后发优先，保存期间则禁止换目标。
- 写操作有独立锁，保留发起时的目标、版本及输入。创建餐点后若离页不继续添加菜品；旧确认框在换日期/标签、修改餐点版本、换家庭或关闭页面后失效，点击确认时再次检查管理权限。正常重开使用弹窗发起时裁剪后的原因。
- 份数接受0.001～100、最多3位小数；多人想吃不由客户端乘份数。确认、开始、缺料导入和完成后重新读取服务端状态；完成仍无任何库存消耗参数。缺料导入要求餐点、库存和购物权限，仅发送当前勾选的缺料键及餐点快照版本，不发送自行计算的缺口数量。
- 分类创建、修改、归档及菜谱状态修改也纳入当前视图/编辑代次检查。保存期间禁用相关表单，迟到修改结果不填回已离开的页面。原分类版本与生命周期回归继续通过。
- 新增22条组件回归，全量167/167通过。包括正常点菜/撤回、两人意愿一份菜的服务端结果展示、份数更新、确认→导入缺料→开始→完成、历史乱序、返回、撤权、旧弹窗和各迟到响应。这些是Vue组件调用模拟API的证据，不替代真实双账号/数据库端到端验收。
- 最终微信生产构建成功，78个文件同步原`outputs/mp-weixin`，SHA256差异0，私有配置保留；产物包含新的加餐确认提示和请求保护逻辑。客户端lint仍未补齐，本轮不宣称lint通过。

本节时点发现的购物/库存缺口已在上方“后续购物与手动库存闭环”处理；服务端字段语义已核对，位置固定、到期日允许清空。该组件回归仍不代替真实数据库及真机闭环验收。

## 后续菜谱编辑与封面闭环（2026-09-10 05:16，客户端145/145）

对应U03、D02/D04/D10、A09/A10/A33/A35/A46。实现提交`71ee3dc`，仅改客户端；没有服务器配置、数据库或后端镜像变更。

- 旧页面只在onLoad读取，无权限刷新和离页清理；已复现只读账号仍请求编辑数据、乱序详情覆盖、重复创建和非法数量发起请求。当前读取先要求recipes:EDIT，按页面代次与家庭/成员身份绑定，离页或换账号清空正文、分类、封面和草稿。
- 保存采用捕获的菜谱ID/版本/完整草稿及单一提交锁。迟到返回不回填或导航；401/403/404清空私有字段；409保留用户草稿和旧版本，不能静默采用新版本覆盖其他人的修改。历史归档分类默认保留、主动改“未分类”传null的既有回归继续通过。
- 食材数量可留空；填写时必须为正十进制、最多3位小数，并符合现有Decimal(12,3)范围。拒绝NaN、科学计数、十六进制、非正数和过精度输入；调料仍无用量。表单在保存或选图期间锁定。
- 封面选择在用户明确操作时保存内存草稿及原身份；选图引起的页面隐藏仍清空可见数据，返回后重新授权再恢复，不持久保存草稿或签名URL。两种返回顺序和取消均有测试；取消不会为了选图而先创建空草稿。
- 验证文件数、JPG/PNG/WebP、0<size<=8MB、实际读取字节数后，新菜才保存一次草稿并上传。失败后保留已成功创建的菜谱ID，用户重选时复用；已存在的菜谱不因选图而偷偷保存未提交的文字。
- 申请上传、传输、确认以及完成后的读取前复核权限、原菜谱及版本。发现并发修改时保留原草稿版本并提示冲突。自身封面确认成功后使用返回的新版本继续编辑，即使预览签名失败也不丢弃已保存的封面。
- 切换目标菜谱、换家庭、撤权、上传阶段隐藏或卸载会停止后续步骤；已发出的网络请求可能在服务器完成，不宣称可以撤回它，也不在旧页面重新显示结果。预览失败可重新签名加载，迟到链接不能填回隐藏页面。
- 新增21条真实Vue setup/模拟网络与原生API测试，全量145/145通过；微信生产构建完成，78个文件已同步原导入目录，逐文件SHA256差异0，保留私有配置。未操作微信平台上传或发布。

本节时点发现的点餐缺口已在上方“后续点餐客户端闭环”实现并补测；这仍不是“录菜到点餐”真实双账号整条链路验收。客户端lint工具配置及真实微信/COS/地图/订阅消息验收仍未完成。

## 后续日历闭环检查（2026-09-09 22:24，客户端124/124）

对应U11、D01/D09、A21/A30/A37/A43。实现提交为`25f0f57`，只修改客户端，不改后端、迁移或生产数据。

- 核对实际注册入口为`pages/index/index`和`pages/date-detail/index`，不是未注册的旧`pages/calendar/index`占位页。
- 修复月历按设备时区计算查询范围的问题。日期格使用UTC纯日期运算、查询边界固定北京时间，42个可见日期（含两侧相邻月份）全部查询。跨日、跨年、闰日及行程结束日半开区间有精确断言。
- 两页的请求按页面代次和家庭/成员身份隔离；刷新前清空家庭数据，隐藏/卸载清空内容，迟到成功/失败不回填、不覆盖新结果、不提前结束新请求的加载状态。没有日历查看权限时不发送业务读取请求；页面失败提供重试，不显示成“没有安排”。
- 纪念日创建/编辑/删除具备提交锁、捕获的ID及版本、编辑代次和身份检查。离页后已发送的请求可能完成，但不恢复旧草稿或显示迟到提示；原请求未结束前返回页面仍禁止重复提交。关闭或更换编辑器、离页、换家庭后，旧删除确认失效。
- 无效日期不发送写请求；编辑清空备注明确传null；409保留原版本草稿供用户处理，401/403清空私有内容。保存期间禁用表单输入、日期及重复规则，防止未提交改动被误认为已保存。
- 原有按模块权限的餐点、行程、待办新建与详情入口保留；只有当前列表中的来源事件可进入对应详情。“每年提醒”改为“每年显示”，年重复日历投影不冒充已接通微信通知。
- 新增19条组件/日期回归，总计124/124通过。默认环境完整回归通过；`TZ=America/Los_Angeles`下19/19通过。测试使用真实Vue setup和模拟网络/微信生命周期，不替代真机或数据库端到端验收。
- `pnpm --filter @family-life/client test`已添加为统一客户端测试入口。微信生产构建成功，78个文件同步到原`outputs/mp-weixin`，逐文件SHA256不一致数0，保留`project.private.config.json`，确认产物API仍为`https://pp6v4.com/api/v1`。
- 验证限制：客户端现有lint脚本因缺少本包ESLint可执行文件而失败，且尚无客户端Vue ESLint配置；未把构建/测试成功当作lint通过。本轮未安装新依赖或改变锁文件。该工程验证缺口仍需补齐。

外部联调和逐需求发布验收仍未完成；本节仅证明上述日历客户端路径，不宣称整个项目闭环。

## 后续露营闭环检查（2026-09-09，客户端回归65/65）

本节按实施顺序保留65、82、105条测试时点。原生返回与子组件生命周期的最新状态见下方“原生交互返回”小节，不应将较早的待修复描述当作当前状态。

本轮修复：

- 节点经纬度空白不再被Number转换成0,0；拒绝非十进制输入和越界坐标，保留真实零坐标的合法录入。
- 节点及行程按上海日期显示，跨UTC午夜、闰日和跨年不依赖设备时区；住宿日期保留纯日历日期语义。
- 仅修改标题/备注时保留原始时间戳和精度，不再统一覆盖为08:00/18:00/20:00；主动改日期才使用原有默认时间，清空可选时间明确提交null。
- 节点备注、住宿地址/联系方式/预订备注清空后提交空字符串，服务端按已有约定清除，不再因undefined保留旧值。
- 客户端拦截不存在的日期、反向时间范围、零晚住宿和已经不存在的关联节点；只读行程不提交残留的节点/住宿表单。
- 新增9条真实组件/辅助函数回归，总计65/65通过；测试验证请求参数与表单状态，不替代原生地图或真机验收。
- 微信生产构建成功，76个文件同步至项目上级outputs/mp-weixin，逐文件SHA256差异0，开发者工具私有配置保持不变。本轮仅客户端变更，没有重启后端或更改生产数据库。

新发现、仍需实现/验证的独立问题（并非全部受凭证阻塞）：

1. 露营父页面loadData/openTrip及子组件异步请求缺少统一的生命周期与请求序号保护；隐藏/换行程后的旧响应可能回填。父页面已在下述后续修复覆盖，子组件自己的选点/选图生命周期仍未闭环。
2. 相册chooseMedia后使用可变化的props.trip，且父页面onShow会重新加载并卸载子组件。需把选图前后的行程/账号身份绑定、权限重新检查和组件销毁处理串起来，再验证预览链接刷新；现有API归属权限检查不能替代正确的客户端目标绑定。
3. 后端accommodationDates只校验Date是否NaN与先后顺序，尚需增加YYYY-MM-DD解析往返一致性，防止直接API调用将2月29日等无效日期归一化到下月。本轮客户端拦截不替代服务端校验。

上述问题尚未修复，不应把本轮65条通过解读为露营端到端已验收。

### 后续：露营父页面异步隔离（客户端82/82）

- 列表、行程详情、候选成员、行李读取及修改结果绑定页面代次、详情代次、标签页、家庭、成员和行程ID；存储中的账号身份变化也会使结果失效。
- 权限刷新、页面隐藏/卸载清空列表、候选人、负责人和编辑草稿。返回只保存行程ID及身份标识，在同一家庭/成员重新授权成功后恢复；不恢复跨家庭详情。
- 详情加载完成前隐藏旧详情；较早成功/失败响应不能覆盖或关闭后来选择的行程。版本更新不倒退。
- 行李修改、负责人分配、成员和小组操作使用发起时的目标；防重复提交；删除、撤权、状态切换及模板归档的确认框在换页/换行程后失效。当前401/403/404响应清除页面私有内容。
- 地图和相册子组件按成员、行程和详情代次重新创建；变更事件携带行程ID，父页面拒绝其他行程的迟到事件。此项不等于子组件内部请求或原生工具返回流程已经修复。
- 新增17条组件回归，覆盖乱序列表/详情/候选人、隐藏/卸载、切换家庭、权限变化、旧弹窗、重复写入及正常创建/成员/小组/完成流程，总计82/82。
- 微信生产构建成功，76个文件同步至原outputs/mp-weixin，SHA256差异0，保留project.private.config.json；未改后端和生产数据库。

下一轮仍须解决：原生chooseLocation、chooseMedia和previewImage导致页面隐藏时，父页面清空/重新创建子组件会使选点草稿丢失，选图上传缺少独立绑定身份和撤权检查。应让这类用户明确发起的原生交互拥有独立的返回状态与重新授权流程，而不是取消离页清理；同时补齐子组件旧请求/销毁回归。后端住宿日期解析和真实外部服务验收也仍待完成。

### 后续：原生交互返回与子组件隔离（客户端105/105）

- 微信选点、选图、预览由露营父页面统一处理。子组件只发出携带行程ID的用户操作事件，不再在被销毁后的回调里读可变化的props并继续上传。
- 每次操作保存原行程ID、家庭ID和成员ID。原生工具导致隐藏时，页面数据继续清空，仅在内存保留该次操作所需的目标和选点草稿；没有写入本地持久存储。卸载、换行程或标签页使操作失效。
- 返回后重新检查家庭权限及明确的行程成员身份，再按原身份恢复。选点取消也能恢复原草稿手工输入；编辑现有节点保留原版本号和未保存内容，不悄悄采用最新版本覆盖并发修改。
- 照片读取、申请上传、传输和确认之间分别复核权限；每张图片使用重新读取的原行程版本。超过9张、空文件、超8MB、格式不支持及读取大小不一致在上传前拒绝。部分失败只报告已确认成功的张数，不自动重传成功项。
- 预览重新列出当前有权查看的相册并签发查看链接，签名后再次核对行程访问权限。过期链接不从旧组件直接传给原生预览。
- 地图和相册子组件的隐藏/销毁、只读变化、旧读取和删除确认都有失效保护；401/403触发匹配行程的父页面清理。已完成行程的有权历史成员仍可补照片，但不能改地图节点。
- 新增23条测试，累计105/105通过。覆盖两种onShow/选择结果返回顺序、取消、换家庭/行程、撤权、卸载、上传各边界、部分失败、刷新版本、预览重新签名，以及真实Vue响应式watch触发的组件销毁/恢复。测试中的原生API及网络为模拟，不代表真机/COS已验收。
- 微信生产构建成功，77个文件同步至原outputs/mp-weixin，逐文件SHA256差异0，保留开发者工具私有配置。新增trip-native.ts被实际产物打包；本轮未修改后端和生产数据库。

接下来尚需完成：服务端住宿日期解析往返校验、真实微信双账号及原生工具真机验证、COS凭证配置与真实写读、地图供应商搜索/道路规划、订阅消息模板与发送联调，以及最终逐需求发布验收。客户端测试通过不替代这些条件。

### 后续：服务端住宿日期严格校验

- 旧实现仅依赖JavaScript Date非NaN判断，2026-02-29、2028-02-30等会自动滚入下月。新增5条后端测试在旧代码上2通过、3失败，已复现创建和修改均可能接受错误日期。
- 已增加YYYY-MM-DD格式及解析往返一致性校验，保留UTC日历日期语义和退房晚于入住约束；未新增数据库迁移，也不推测或改写历史数据。
- 本地API构建、ESLint、住宿5/5与通知6/6单元测试、验收运行器3/3测试通过。
- 服务器隔离快照20260909T091556Z-c894f306，项目kk-verify-362b950ff3bb706b：18批迁移、零结构差异、旧数据回归6/6、通知6/6、住宿5/5、HTTP51/51通过，退出码0。新增HTTP用例直接检查无效创建/更新后住宿行、行程版本/更新时间和审计数量均不变；合法闰日、清空备注及过期版本409均按预期。
- 快照包含232个源码文件，基于e337281加本轮明确修改，逐文件SHA256在本地.codex-tmp/remote-db-verification/20260909T091556Z-c894f306/source-manifest.json；不是e337281原提交单独通过了新测试。
- 后续已构建正式runtime镜像并通过启动冒烟，版本40dde83于北京时间22:04完成生产切换。实际API/worker镜像一致且重启次数0，HTTPS复查通过；运行容器中住宿单元测试5/5通过（模拟数据库，无生产业务测试写入）。备份、摘要和执行证据见[最新部署记录](deployment-20260909.md)。本轮无新增迁移、无生产数据库重置。

## 数据库验收证据（2026-09-09；下列旧镜像描述为部署前快照）

后续已完成[真实备份恢复与带数据升级验证](upgrade-verification.md)：真实备份可恢复，但23张业务表均为空，另用虚构旧数据覆盖5个升级场景。补回跨日/闰日纪念日转换回归并修正尚未在生产执行的迁移SQL后，最新快照`20260908T223148Z-76af697f`再次通过18批迁移、零结构差异、旧数据6/6（父测试加5个场景）、通知6/6和HTTP50/50。以下较早快照作为历史证据保留，最新部署应使用该后续修复。

- 隔离数据库闭环：18批迁移deploy/status成功；Prisma模型对照 `No difference detected`；全部50条HTTP用例通过，无跳过。覆盖以实际测试断言为限，不是对所有并发、负载和外部服务的完整证明。
- 通知worker：读取来源、权限和写入放在同一Serializable事务；旧领取标记不覆盖新任务；用户显式关闭安静时段后夜间可送达；临时错误重试、不误当撤权取消。6条针对性单元测试与A41数据库用例通过。
- 修复Docker安全过滤规则误排除private_media业务迁移；仍保留.env、私钥和临时目录过滤。修复分类用例与六个系统默认分类冲突，保留默认分类的重复名409验证。
- 第18批向前迁移对齐四个外键更新规则及Trip.updatedAt默认值，不改旧迁移文件、不清空数据。空库迁移验证通过；已有生产数据的升级与备份恢复尚待验收。
- 通过快照：`20260908T170219Z-cc00b088`（UTC目录名）；Compose项目`kk-verify-88825e70690cbfc8`；完整证据位于`.codex-tmp/remote-db-verification/20260908T170219Z-cc00b088/`，源文件清单225项及SHA256已保存。镜像manifest摘要`sha256:30affb2d3dea2701e728563183120a54a3d29cc9a534266b7cfb95ccd862df65`。源码归档未带.git，报告Git字段为null；源版本及未提交改动必须结合source-manifest.json核对，不能声称只测试了2855120原始提交。
- 前两轮失败证据保留：第一轮16批迁移遗漏媒体表，接口34/50通过；第二轮补回17批后被新增结构检查正确拦截；第三轮修正后全部通过。未删除停止的测试容器或生产数据。
- 网络与GitHub已恢复；连接方式已按用户要求写入授权的记忆目录。业务API仍是旧镜像，不能将隔离测试结果表述为已上线。

## 历史功能覆盖矩阵（9月7日基线，不代表当前部署状态）

以下矩阵保留原核对基线。其中数据库“待验证”、生产“未部署”和档案密钥“未生成”等状态已由9月9日的测试及[部署记录](deployment-20260909.md)更新；外部服务、真机和双账号验收仍未完成。

| 领域 | 源码状态 | 已验证 | 尚未闭环 |
| --- | --- | --- | --- |
| 登录、共享家庭、成员与权限 A01～A08/A44/A45 | 15分钟访问令牌、30天轮换刷新会话、退出/重用撤销、邀请兑换、本人显示名、角色叠加、显式ALLOW/DENY、停用、管理员转让和服务端家庭隔离已实现 | API/客户端编译；续期与显示名客户端测试通过；HTTP集成用例已编写 | 新增迁移未在隔离库回归；真实微信登录和双账号未验收 |
| 菜谱 A09/A10/A46 | 分类创建/改名/排序/软归档、版本冲突、家庭内重复名409、未分类菜谱、草稿、编辑、发布/下架/恢复、结构化食材/无用量调料、私有封面已实现；归档分类在旧菜谱编辑时默认保留，主动切换才改变 | Prisma客户端生成、API/客户端编译；分类、归档分类编辑和状态客户端测试通过；图片格式单元测试；HTTP用例已编写 | 第17批分类迁移及HTTP用例待隔离PostgreSQL执行；真实COS和真机选图待验收 |
| 点餐、快照、库存辅助 A11～A18 | 同餐唯一、多人同菜一份、份数、确认快照、全部食材/调料比对、缺料标色已实现 | 客户端交互测试；HTTP集成用例已编写 | 数据库集成回归待执行；一期明确不做自动增减库存 |
| 购物 A19/A20 | 愿望/下次超市/补货、缺料导入幂等、购买历史和复购已实现 | 客户端版本/重试测试；HTTP集成用例已编写 | 数据库集成和真机验收待执行；购买不自动入库 |
| 日历与纪念日 A21/A30/A37/A43 | 餐点、本人有权行程、待办按来源实时聚合；纪念日支持一次/每年重复、2月29日策略、编辑和软删除；跨模块详情及新建入口按对应模块VIEW/EDIT权限保护 | API/客户端编译；跨模块入口权限客户端测试通过；HTTP用例已编写 | 农历和单次例外不在一期；数据库迁移与真机交互待验收 |
| 露营成员与行李 A21～A29 | 行程显式成员、历史保留/主动撤权、OWNER、小组分工、模板和模板项版本控制、模板项软归档、行程快照、负责人与状态已实现 | API/客户端编译；HTTP用例已编写 | 独立数据库和多账号真机协作待验收 |
| 路线、交通、住宿 A30～A32 | 有序节点、GCJ-02、交通方式、住宿、示意/规划路线区分、状态线型已实现 | API/客户端编译 | 地图供应商搜索和真实道路规划未接入；原生地图动画需真机验收 |
| 图片 A33～A35 | 同域API上传、归属校验、私有对象、60秒读取链接、菜谱封面/行程照片/收藏图片已实现 | 图片签名单元测试通过 | 真实COS写读、孤儿对象清理和真机批量图片待验收 |
| 家庭待办/请求 A36 | 负责人、截止/提醒、优先级、状态、重开原因、历史评论、日历联动已实现；客户端在发送前校验并裁剪重开原因，服务端重复校验 | API/客户端编译；待办重开客户端测试通过；HTTP用例已编写 | 任务附件不在当前一期；数据库集成待执行 |
| 收藏与灵感 A38 | 文字/图片/链接、个人/家庭可见、编辑归档、幂等转换菜谱草稿/待办已实现；页面刷新/隐藏清空内容，过期响应不回填；选图返回重新校验账号/家庭/编辑权限，预览重新签名读取链接 | 客户端正常保存、乱序详情、选图返回/取消、撤权、切换家庭、卸载、后台完成、预览返回及转换重试测试通过；HTTP用例已编写 | 真实COS和数据库集成待执行；微信真机选图及预览生命周期待验收；不自动抓网页或猜食材 |
| 家庭档案 A39 | 自定义字段、逐字段ACL、AES-256-GCM、版本和脱敏审计已实现；权限刷新及页面隐藏/卸载立即清空明文，过期读取/保存响应不再回填；切换家庭不恢复原字段 | 客户端生产构建；撤权、迟到响应、切换家庭、正常保存和离页保存5条回归通过；HTTP用例已编写 | 生产加密密钥的生成、备份和轮换演练；数据库集成/真机验收 |
| 生活看板 A40 | 按来源权限聚合菜谱、餐次、常选菜、购物、可见行程/行李和待办完成率 | API/客户端编译；客户端参数测试通过 | 数据库口径回归待执行；明确不含账本、预算和成员排名 |
| 消息提醒 A41 | 待办事务outbox、独立worker、版本/权限复核、安静时段、重试、站内Inbox已实现 | API/客户端编译；客户端交互测试；HTTP用例已编写 | 微信订阅消息真实发送未实现，需模板ID/字段和真机联调；数据库worker回归待执行 |
| 部署 A42 | Docker多阶段镜像、Nginx单域名`/api/`反代、PostgreSQL/Redis内网、API/COS外网、迁移启动、域名首页和`辽ICP备2026020161号-1`工信部链接已配置 | 9月9日SSH恢复、HTTPS证书校验、首页200、健康接口200、未登录菜谱401、数据库SELECT 1、Redis健康、API外网访问和GitHub仓库读取通过；证书续期timer已启用 | 最新业务代码未部署；17批迁移未在隔离库执行；小程序平台备案、公安备案、真实微信/COS凭证与业务联调、备份恢复待完成 |

## 2026-09-03 本地验证快照

- API ESLint与TypeScript构建：通过。
- Prisma schema：通过（使用仅供校验的本地占位DATABASE_URL，不连接数据库）。
- 微信小程序构建：通过；构建结果已同步到`outputs/mp-weixin`并做文件哈希核对。
- 客户端逻辑与媒体签名单元测试：32/32通过。
- 数据库HTTP集成测试：50条声明已编写，本轮未执行；不能记录为PASS。
- Prisma迁移目录：17批；本轮未在独立PostgreSQL执行。

## 2026-09-07 补充证据

- 修复家庭档案页面已展开的明文在权限刷新后仍残留，以及离开页面后异步响应重新填回的问题。保存后重新获取可见字段及版本，内容回到遮罩状态。
- 客户端逻辑与媒体签名测试：37/37通过，其中新增5条覆盖上述隐私和保存路径。微信小程序生产构建通过，75个文件同步到`outputs/mp-weixin`，SHA256不一致数0，保留`project.private.config.json`。
- 使用现有项目验证密钥通过Paramiko连接`ubuntu@49.233.18.181`，服务器返回`Authentication failed`。尚未执行远程命令、数据库迁移或部署。
- 后续修复待办、看板、消息页：权限刷新及隐藏/卸载清空页面；过期列表、详情、修改结果不回填；消息已读请求返回后不再跳回已离开的页面；看板按最新日期范围显示，菜谱独立查看权限可以进入菜谱入口。新增7条回归后累计44/44通过。
- 后续补齐收藏页：列表和详情刷新、编辑/归档/转换返回均按页面版本丢弃过期响应；编辑要求favorites:EDIT与创建者/管理员身份同时满足；选图期间隐藏只保留目标ID及家庭/成员ID，返回后校验身份权限再上传，卸载中止后续步骤；预览使用重新签发的图片链接。新增11条用例后客户端累计55/55通过。该证据模拟生命周期顺序，不替代真机相册、相机、预览和真实COS验收。
- 补齐隔离数据库容器验收入口：`node scripts/verify-database.cjs`，使用独立Compose文件、随机项目/数据库地址和tmpfs，按顺序执行容器构建、全部迁移及HTTP集成。记录源码提交、工作区状态、阶段退出码和TAP日志；不读取生产env，不发布数据库端口，不挂载生产卷。加强迁移和集成测试入口的数据库地址检查，并把`.codex-tmp`排除出Docker构建上下文。
- 运行器自身3条测试通过；YAML解析及隔离结构检查通过。实际运行`kk-verify-93803bde6ce2253d`在Docker检查阶段返回ENOENT，未构建容器、未执行任何迁移或HTTP用例。完整记录在`.codex-tmp/db-verification/93803bde6ce2253d/`。这项工具验证不能替代50条业务集成测试。

## 下一步顺序

9月9日网络问题已定位并修复：之前验证使用了错误的临时密钥；真实部署密钥已多次连接成功。线上仍为HTTP bootstrap且无证书，现已签发证书并切换HTTPS。API原来仅连接internal网络，现已添加edge并持久化到线上Compose。数据库与Redis仍无宿主机公开端口。参见[网络恢复记录](network-recovery-status.md)。此轮没有更新业务镜像或执行业务迁移，不能把网络恢复等同功能验收。

1. [隔离数据库验收](../infra/verification/README.md)、真实备份恢复、虚构旧数据升级及生产部署均已通过。生产升级前业务表为空，不能把副本验证夸大为大量真实数据迁移验收。
2. 配置真实COS凭证，完成微信双账号、真机媒体/权限/站内提醒验收。微信凭证和档案密钥已加载，但配置存在不等于真实联调成功；档案密钥仍需服务器外安全备份。
3. HTTPS和ICP备案首页已经就绪，不再列为网络阻塞。小程序平台备案、公安备案等条件确认后再走发布验收。微信订阅发送和地图供应商真实规划仍需对应模板ID及Key。

发布判定仍以[`design/acceptance.md`](design/acceptance.md)保存的输入、输出、日期和环境记录为准。
